= form_for @generic_upload do |f|
  .form-group
    .custom-file
      = f.file_field :binary, multiple: false, class: "custom-file-input"
      %label.custom-file-label{:for => "binary"} Choose file
  .form-group
    = f.submit "Upload", class: 'btn btn-primary'

:javascript
  $('#generic_upload_binary').on('change',function(){
      //get the file name
      var fileName = $(this).val().replace("C:\\fakepath\\", "");
      //replace the "Choose a file" label
      $(this).next('.custom-file-label').html(fileName);
  })
