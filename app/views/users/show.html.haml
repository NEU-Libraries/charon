%h2.py-2
  Profile for
  = @user.name

%table.table.table-bordered.table-responsive
  %tbody
    - @assignments.each do |action|
      %tr
        %td.align-middle
          = render partial: "shared/actions/display", locals: {action: action}

%table.table.table-bordered.table-responsive
  %tbody
    - @user_actions.each do |action|
      %tr
        %td.align-middle
          = render partial: "shared/actions/display", locals: {action: action}
