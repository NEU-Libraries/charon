= simple_form_for(@change_set) do |f|
  = f.input :title
  = f.input :description, as: :text, :input_html => { :rows => 10 }
  = f.input :manager, collection: User.all.sort_by(&:last_name)
  = f.button :submit, class: 'btn btn-primary'
