%h2.py-3
  Notifications

%table.table.table-bordered.table-responsive
  %thead
    %tr
      %th{:scope => "col"} Sent
      %th{:scope => "col"} Subject
      %th{:scope => "col"} Message
  %tbody
    - @notifications.each do |notification|
      = render 'notification_row', :notification => notification, :unread => @unread_ids.include?(notification.id)

%a.btn.btn-primary.btn-lg.float-right{:href => mark_all_notifications_as_read_url, :role => "button"} Mark All As Read
