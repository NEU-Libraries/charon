# Use postgres/example user/password credentials
version: '3.7'
services:
  sidekiq:
    build: .
  web:
    build: .
    volumes:
      - ./:/home/charon/web
      - storage:/home/charon/storage
      - images:/home/charon/images
